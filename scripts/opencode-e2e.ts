/** OpenCode was retired; this old smoke command must never provision a run. */
throw new Error(
  "OpenCode Build has been retired. Run the native harness suites in lib/api/__tests__/agent-stream-runner.loop.test.ts instead.",
);
export {};
