import { SettingsIndex } from "@/app/components/settings/SettingsIndex";

/**
 * The section list. On a phone this is the whole first screen of settings —
 * you reach a section by navigating to it rather than by scrolling past nine
 * nav tiles to whatever the dialog happened to have selected.
 */
export default function SettingsPage() {
  return <SettingsIndex />;
}
