/** Canvas dither wash; see dither-kit-sparkline mock for why. */
export function DitherGradient({ className }: { className?: string }) {
  return <div data-testid="dither-gradient" className={className} />;
}
