# CLI input and exit checks — 2026-09-13

The installed `/Users/cetto/.local/bin/rift` reports 0.3.5. `rift --json doctor
--online` confirmed authenticated access to localhost:3020 and ten models. A fresh
isolated directory was opened through a real PTY: the welcome screen and slash
command menu appeared, then Ctrl+C exited with code 0 while the menu was open.
No model task was submitted through that PTY. This is not a clean-machine install
or proof of every CLI feature. The installed binary's source identity was not
inferred from its version.

Current source checks: 56 console tests passed; 11 native OpenTUI tests passed
(54 assertions), including idle Ctrl+C and stop-then-exit while work is pending.

The performance script previously entered the draft before streaming and only
checked preservation. It now sends ten additional keyboard inputs per terminal
size during the 100-update replay, checks exact text after every update and input,
and checks the final rendered text. All three sizes passed: 80×24, 126×46, 180×60.
The fixture contains 1000 historical entries of 30 lines and 1000 initial live
lines. Raw results are in `2026-09-13-cli-input.json` alongside this report.

`inputAndVisualIdleMs` includes the OpenTUI test renderer's visual-idle scheduling
wait at targetFps 60. It is not OS key-to-photon latency; the test excludes PTY
transport, terminal emulator compositing, model calls and reconnect latency.
Only ten input samples per size are collected: no percentile SLO or competitor
parity can be established from these samples.

Reproduce from packages/console:

```sh
pnpm test
bun test test/opentui.test.ts
bun run scripts/verify-performance.ts
```
